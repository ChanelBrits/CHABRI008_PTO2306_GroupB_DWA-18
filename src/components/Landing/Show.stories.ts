import { Show } from "./Show"
import { Meta, StoryObj } from "@storybook/react"

const meta:Meta<Show> = {
    title: "components/Show",
    component: Show,
}

export default meta;

export const Base: StoryObj = {
    args: {
        
    }
}