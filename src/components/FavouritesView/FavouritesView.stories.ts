import { FavouritesView } from "./FavouritesView"
import { Meta, StoryObj } from "@storybook/react"

const meta:Meta = {
    title: "components/FavouritesView",
    component: FavouritesView,
}

export default meta;

export const Base: StoryObj = {
    args: {
    
    }
}